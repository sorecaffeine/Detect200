[project]
name = "downdetector"
version = "0.1.0"
description = "Simple service availability monitor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "fastapi[standard]",
]

[tool.uv]
package = false
